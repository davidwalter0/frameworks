/// General-purpose ordered fuzzy matching for desktop_kit — not editor
/// specific, usable by any completion/filter UI (file paths, command
/// palettes, …).
///
/// Opt-in, like the anthy/kana bridges: NOT re-exported from `desktop_kit.dart`.
library;

export 'src/ordered_match/ordered_match.dart';
